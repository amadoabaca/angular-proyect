import { CharacterServiceTsDirective } from './character.service.ts.directive';

describe('CharacterServiceTsDirective', () => {
  it('should create an instance', () => {
    const directive = new CharacterServiceTsDirective();
    expect(directive).toBeTruthy();
  });
});
